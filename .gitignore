# Dependencies - restored by npm ci in the workflow
node_modules/

# Generated output - rebuilt by the workflow on every run and deployed
# to GitHub Pages as an artifact, so it never needs to be versioned
public/

# Local notes and idea documents - personal, not part of the public repo
docs/

# Notification state - runtime only, restored from the Actions cache
.notify-state.json
